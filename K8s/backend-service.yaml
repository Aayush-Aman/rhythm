apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: my-app        # This MUST match the label in your deployment.yaml
  ports:
    - protocol: TCP
      port: 80         # The port accessible from the outside world
      targetPort: 80   # The port your container is actually listening on
  type: LoadBalancer